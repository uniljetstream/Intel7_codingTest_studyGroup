#include <iostream>
#include <queue>
#include <algorithm>

using namespace std;

bool cmp(const int& a, const int& b)
{
    return a < b;
}

int solution(vector<int> scoville, int K)
{
    int answer = 0;

    return answer;
}

int main()
{
    solution({1, 2, 3, 9, 10, 12}, 7);
    return 0;
}