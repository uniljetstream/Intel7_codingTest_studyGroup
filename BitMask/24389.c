#include <stdio.h>

int main()
{
    int ari = 1023;
    int s, m, calc;
    scanf("%d %d", &s, &m);
    if (s <= ari)
    {
        printf("%s", "No thanks");
        return 0;
    }
    
    calc = s - ari;
    if
}